<%= form_for(@star_system) do |f| %>
  <% if @star_system.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@star_system.errors.count, "error") %> prohibited this star_system from being saved:</h2>

      <ul>
      <% @star_system.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
